query stopsByWorkspaceId($workspaceId: String) {
  signedUser {
	joinedWorkspaces(id: $workspaceId) {
	  edges {
		node {
		  stationGroups {
			edges {
			  node {
				  ... StopFragment
			  }
			}
		  }
		}
	  }
	}
  }
}
