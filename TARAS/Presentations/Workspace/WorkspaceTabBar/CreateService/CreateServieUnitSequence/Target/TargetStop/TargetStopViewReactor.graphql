query stopsList($first: Int, $swsIdx: Int!) {
    swsStopsConnection(first: $first, swsIdx: $swsIdx) {
        edges {
            node {
                name
                stopIdx
            }
        }
    }
}
