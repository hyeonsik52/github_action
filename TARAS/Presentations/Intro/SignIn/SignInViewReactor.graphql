#REST로 대체
#mutation signIn($input: LoginInput!) {
#	loginMutation(input: $input) {
#		... on LoginError {
#			errorCode
#		}
#		... on LoginPayload {
#			accessToken
#			refreshToken
#		}
#	}
#}

query myUserInfo {
	signedUser {
		... UserFragment
	}
}

fragment UserFragment on UserNode {
	id
	username
	displayName
	email
	phoneNumber
}
